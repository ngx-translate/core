import {Parser} from '../src/translate.parser';

export function main() {
    describe('Parser', () => {
        it('is defined', () => {
            expect(Parser).toBeDefined();
        });
    });
}